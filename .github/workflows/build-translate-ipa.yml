name: Build RPGTranslate IPA (unsigned)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'TranslateTool/**'
      - '.github/workflows/build-translate-ipa.yml'

concurrency:
  group: build-translate-ipa
  cancel-in-progress: false

jobs:
  build:
    runs-on: macos-15
    defaults:
      run:
        working-directory: TranslateTool
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build unsigned (device)
        run: |
          set -euo pipefail
          xcodebuild \
            -project RPGTranslate.xcodeproj \
            -scheme RPGTranslate \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package IPA
        run: |
          set -euo pipefail
          mkdir -p Payload
          cp -R "build/Build/Products/Release-iphoneos/RPGTranslate.app" Payload/
          zip -ry RPGTranslate-unsigned.ipa Payload

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: RPGTranslate-unsigned-ipa
          path: TranslateTool/RPGTranslate-unsigned.ipa
          if-no-files-found: error
